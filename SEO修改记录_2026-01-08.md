# 对话修改记录

> **对话日期**: 2026-01-08
> **文件创建时间**: 2026-01-09

---

## 2026-01-08

### 1. 域名与 Canonical 配置修复

- **修改文件**: `app/layout.tsx`
- **修改内容**:
  - `metadataBase` 从 `https://www.goodokshop.com` 改为 `https://goodokshop.com`
  - 注释掉固定的 `canonical: "/"` 配置
  - 在 `openGraph` 和 `twitter` 中添加默认图片 `/images/og-image.jpg`

### 2. Robots.ts 域名修复

- **修改文件**: `app/robots.ts`
- **修改内容**: `sitemap` 地址改为 `https://goodokshop.com/sitemap.xml`

### 3. Sitemap 扩展

- **修改文件**: `app/sitemap.ts`
- **修改内容**:
  - `baseUrl` 改为 `https://goodokshop.com`
  - 新增路由: `/projects`、`/products/checkout-counters`、`/solutions/stationery`、`/solutions/lifestyle`、`/solutions/jewelry-store`

### 4. ProductSchema 修复

- **修改文件**: `components/schema/index.tsx`
- **修改内容**:
  - 新增 `price` 和 `currency` 可选参数
  - 移除强制 `price: "0"` 默认值
  - 只有传入 `price` 时才生成 `offers` 块

### 5. 博客页面 Schema 增强

- **修改文件**: `app/resources/blog/page.tsx`
- **修改内容**: 添加 `BreadcrumbSchema` 组件

- **修改文件**: `app/resources/blog/how-to-open-boutique/page.tsx`
- **修改内容**: 添加 `ArticleSchema` 组件，"Clothing Racks" 添加产品页内链

### 6. 项目页面标题优化

- **修改文件**: `app/projects/boutique-accessories/page.tsx`
- **修改内容**: `title` 和 `H1` 改为 "Fashion Boutique Fixtures - Tokyo, Japan"

### 7. 新建项目案例页

- **新建文件**: `app/projects/stationery/page.tsx` (文具店)
- **新建文件**: `app/projects/lifestyle/page.tsx` (生活馆)
- **新建文件**: `app/projects/jewelry/page.tsx` (珠宝店)

---

## 2026-01-09 (用户手动修改)

### 8. Display Cases 页面乱码修复

- **修改文件**: `app/products/display-cases/page.tsx`
- **修改内容**:
  - `Display Cases 鈥?Glass Showcases` → `Display Cases & Glass Showcases`
  - `鈥攊t makes customers` → `- it makes customers`
  - 添加居中样式 `mx-auto text-center`

### 9. 首页新增跑马灯组件

- **修改文件**: `app/page.tsx`
- **修改内容**:
  - 导入 `MarqueeGallery` 组件
  - 新增 `marqueeImages` 数据
  - 在 Hero 下方插入跑马灯展示
  - 移除首页底部联系表单

### 10. 项目页面重写 (用户手动)

- **重写文件**: `app/projects/jewelry/page.tsx`
- **重写文件**: `app/projects/stationery/page.tsx`
- **修改内容**: 采用新的布局模板，包含项目详情侧边栏

---

## 待办 / 后续计划

- [ ] 创建 `/images/og-image.jpg` (1200x630) 社交分享图片
- [ ] 验证 DNS 配置，确保 `goodokshop.com` 为首选域名
- [ ] 在 Google Search Console 更新首选域名并重新提交 sitemap
- [ ] 为其他博客文章页面添加 ArticleSchema
